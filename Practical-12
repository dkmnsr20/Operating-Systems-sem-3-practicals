# Practical 12: Write a Program to Implement First-Fit, Best-Fit, and Worst-Fit Allocation Strategies

------------------------------------------------------------
SECTION 1: C CODE  (Save as practical12.c)
------------------------------------------------------------

#include <stdio.h>

void firstFit(int blockSize[], int m, int processSize[], int n) {
    int allocation[n];
    for (int i = 0; i < n; i++)
        allocation[i] = -1;

    for (int i = 0; i < n; i++) {   
        for (int j = 0; j < m; j++) { 
            if (blockSize[j] >= processSize[i]) {
                allocation[i] = j;
                blockSize[j] -= processSize[i];
                break;
            }
        }
    }

    printf("\nFirst Fit Allocation:\n");
    for (int i = 0; i < n; i++) {
        if (allocation[i] != -1)
            printf("P%d -> Hole %d\n", i + 1, allocation[i] + 1);
        else
            printf("P%d -> Not Allocated\n", i + 1);
    }
}

void bestFit(int blockSize[], int m, int processSize[], int n) {
    int allocation[n];
    for (int i = 0; i < n; i++)
        allocation[i] = -1;

    for (int i = 0; i < n; i++) {
        int bestIndex = -1;
        for (int j = 0; j < m; j++) {
            if (blockSize[j] >= processSize[i]) {
                if (bestIndex == -1 || blockSize[j] < blockSize[bestIndex])
                    bestIndex = j;
            }
        }
        if (bestIndex != -1) {
            allocation[i] = bestIndex;
            blockSize[bestIndex] -= processSize[i];
        }
    }

    printf("\nBest Fit Allocation:\n");
    for (int i = 0; i < n; i++) {
        if (allocation[i] != -1)
            printf("P%d -> Hole %d\n", i + 1, allocation[i] + 1);
        else
            printf("P%d -> Not Allocated\n", i + 1);
    }
}

void worstFit(int blockSize[], int m, int processSize[], int n) {
    int allocation[n];
    for (int i = 0; i < n; i++)
        allocation[i] = -1;

    for (int i = 0; i < n; i++) {
        int worstIndex = -1;
        for (int j = 0; j < m; j++) {
            if (blockSize[j] >= processSize[i]) {
                if (worstIndex == -1 || blockSize[j] > blockSize[worstIndex])
                    worstIndex = j;
            }
        }
        if (worstIndex != -1) {
            allocation[i] = worstIndex;
            blockSize[worstIndex] -= processSize[i];
        }
    }

    printf("\nWorst Fit Allocation:\n");
    for (int i = 0; i < n; i++) {
        if (allocation[i] != -1)
            printf("P%d -> Hole %d\n", i + 1, allocation[i] + 1);
        else
            printf("P%d -> Not Allocated\n", i + 1);
    }
}

int main() {
    int m, n;

    printf("Enter number of holes: ");
    scanf("%d", &m);
    int holes[m];

    printf("Enter hole sizes: ");
    for (int i = 0; i < m; i++)
        scanf("%d", &holes[i]);

    printf("Enter number of processes: ");
    scanf("%d", &n);
    int processes[n];

    printf("Enter process sizes: ");
    for (int i = 0; i < n; i++)
        scanf("%d", &processes[i]);

    int ff[m], bf[m], wf[m];
    for (int i = 0; i < m; i++) {
        ff[i] = bf[i] = wf[i] = holes[i];
    }

    firstFit(ff, m, processes, n);
    bestFit(bf, m, processes, n);
    worstFit(wf, m, processes, n);

    return 0;
}

------------------------------------------------------------
SECTION 2: LINUX TERMINAL COMMANDS
------------------------------------------------------------

gcc -o practical12 practical12.c
./practical12

# Enter these inputs when asked:
# Holes: 3
# Hole sizes: 5 10 20
# Processes: 3
# Process sizes: 3 8 15

------------------------------------------------------------
SECTION 3: EXPECTED INPUT / OUTPUT
------------------------------------------------------------

Input:
Enter number of holes: 3
Enter hole sizes: 5 10 20
Enter number of processes: 3
Enter process sizes: 3 8 15

Expected Output:

First Fit Allocation:
P1 -> Hole 1
P2 -> Hole 2
P3 -> Hole 3

Best Fit Allocation:
P1 -> Hole 1
P2 -> Hole 2
P3 -> Hole 3

Worst Fit Allocation:
P1 -> Hole 3
P2 -> Hole 3
P3 -> Hole 2

------------------------------------------------------------
NOTES:
------------------------------------------------------------
- First Fit chooses the first available hole.
- Best Fit chooses the smallest adequate hole.
- Worst Fit chooses the largest adequate hole.
- Make sure to copy hole arrays before each method (done in code).
