# Practical 8: Write a Program to Implement First Come First Serve (FCFS) Scheduling Algorithm

## Section 1: C Code
# Save this as 'practical8.c'
# Simulates FCFS; inputs n, arrivals, bursts.

#include <stdio.h>

struct Process {
    int id, arrival, burst;
};

int main() {
    int n;
    printf("Enter number of processes: ");
    scanf("%d", &n);

    struct Process p[n];
    for (int i = 0; i < n; i++) {
        p[i].id = i + 1;
        printf("Enter arrival time for P%d: ", i + 1);
        scanf("%d", &p[i].arrival);
        printf("Enter burst time for P%d: ", i + 1);
        scanf("%d", &p[i].burst);
    }

    // Sort by arrival time
    for (int i = 0; i < n - 1; i++) {
        for (int j = 0; j < n - i - 1; j++) {
            if (p[j].arrival > p[j + 1].arrival) {
                struct Process temp = p[j];
                p[j] = p[j + 1];
                p[j + 1] = temp;
            }
        }
    }

    printf("\nProcess\tArrival\tBurst\tCompletion\tTurnaround\tWaiting\n");
    int current_time = 0;
    float total_tat = 0, total_wt = 0;
    for (int i = 0; i < n; i++) {
        if (current_time < p[i].arrival) current_time = p[i].arrival;
        int completion = current_time + p[i].burst;
        int tat = completion - p[i].arrival;
        int wt = tat - p[i].burst;
        total_tat += tat;
        total_wt += wt;

        printf("P%d\t%d\t%d\t%d\t\t%d\t\t%d\n", p[i].id, p[i].arrival, p[i].burst, completion, tat, wt);
        current_time = completion;
    }

    printf("\nAvg Turnaround Time: %.2f\nAvg Waiting Time: %.2f\n", total_tat / n, total_wt / n);
    return 0;
}

## Section 2: Linux Terminal Commands
# Run these in Ubuntu/Linux terminal

gcc -o practical8 practical8.c
./practical8  # Enter: 3 processes, arrivals 0 1 2, bursts 5 3 8

## Section 3: Expected Output/Input
# Input (prompts):
Enter number of processes: 3
Enter arrival time for P1: 0
Enter burst time for P1: 5
... (for P2: 1,3; P3:2,8)

# Expected Output:
Process Arrival Burst Completion Turnaround Waiting
P1      0      5     5          5          0
P2      1      3     8          7          4
P3      2      8     16         14         6

Avg Turnaround Time: 8.67
Avg Waiting Time: 3.33

# Notes:
# - FCFS: Order by arrival; non-preemptive.
# - Test with different inputs for avg times.
